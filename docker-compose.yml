services:
  app:
    build: .
    ports:
      - "16888:16888"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      # 如需汇率自动刷新，取消下面的注释并填入 API key
      # - EXCHANGERATE_API_KEY=your_key_here
